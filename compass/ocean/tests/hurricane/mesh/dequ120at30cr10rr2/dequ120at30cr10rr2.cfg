# options for global ocean testcases
[global_ocean]

mesh_cores = 18
mesh_min_cores = 1

## config options related to the initial_state step
# number of cores to use
init_cores = 36
# minimum of cores, below which the step fails
init_min_cores = 8 
# maximum memory usage allowed (in MB)
init_max_memory = 1000
# maximum disk usage allowed (in MB)
init_max_disk = 1000
init_threads = 1 

## config options related to the forward steps
# number of cores to use
forward_cores = 128
# minimum of cores, below which the step fails
forward_min_cores = 36
# maximum memory usage allowed (in MB)
forward_max_memory = 1000
# maximum disk usage allowed (in MB)
forward_max_disk = 1000
forward_threads = 1

## metadata related to the mesh
# the prefix (e.g. QU, EC, WC, SO)
prefix = DE 
# a description of the mesh and initial condition
mesh_description = MPAS Delaware Bay refined mesh with quasi uniform 
                   global resolution (${max_res}) enhanced resolution 
                   in the western Atlantic (30km), further increased
                   coastal resolution along the mid-Atlantic coastline
                   (10km) and high resolution in Delaware Bay (${min_res})
# E3SM version that the mesh is intended for
e3sm_version = 2 
# The revision number of the mesh, which should be incremented each time the
# mesh is revised
mesh_revision = 3 
# the minimum (finest) resolution in the mesh
min_res = 2
# the maximum (coarsest) resolution in the mesh, can be the same as min_res
max_res = 120
# The URL of the pull request documenting the creation of the mesh
pull_request = <<<Missing>>>

author = autodetect
email = autodetect
