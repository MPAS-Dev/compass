#!/bin/bash
#SBATCH  --job-name={{ EXP }}
#SBATCH  --account=m1795
#SBATCH  --nodes=5
#SBATCH  --tasks-per-node=64
#SBATCH  --exclusive
#SBATCH  --time=15:00:00
#SBATCH  --qos=regular
#SBATCH  --constraint=haswell

LOGDIR=previous_logs_`date +"%Y-%m-%d_%H-%M-%S"`
mkdir $LOGDIR
cp log* $LOGDIR

source {{ LOAD_SCRIPT }}
date
srun -n 320 --cpu-bind=cores --hint=multithread ./landice_model
date
